"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function define(Python) {
    Python['import_otto'] = function () {
        return 'import otto\n';
    };
    Python['otto_home'] = function (block) {
        return 'otto.home()\n';
    };
    Python['otto_init'] = function (block) {
        var yl = block.getFieldValue('YL');
        var yr = block.getFieldValue('YR');
        var rr = block.getFieldValue('RR');
        var rl = block.getFieldValue('RL');
        var load_calibration = block.getFieldValue('load_calibration');
        var noisesensor = block.getFieldValue('NoiseSensor');
        var buzzer = block.getFieldValue('Buzzer');
        var ustrigger = block.getFieldValue('USTrigger');
        var usecho = block.getFieldValue('USEcho');
        return 'otto.init(${yl}, ${yr}, ${rl}, ${rr}, ${load_calibration}, ${noisesensor}, ${buzzer}, ${ustrigger}, ${usecho})\n';
    };
    Python['otto_walk'] = function (block) {
        var steps = block.getFieldValue('steps');
        var time = block.getFieldValue('time');
        var dir = block.getFieldValue('dir');
        return 'otto.walk(${steps}, ${time}, ${dir}\n';
    };
}
exports.default = define;
